# syncBackup
That sync sourcepath &amp; destinationpath.

Change your source- and targetpath and run it (linux) `python3 syncbackup.py` or (Windows) `python syncbackup.py`

# Requirements 
install python3: https://www.python.org/ 
`pip3 install dirsync`
